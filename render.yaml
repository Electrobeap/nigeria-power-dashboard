services:
  - type: web
    name: nigeria-power-grid-monitor
    env: python
    buildCommand: python -m pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 2 --timeout 60 --bind 0.0.0.0:$PORT
    autoDeploy: true
